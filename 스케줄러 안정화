Improve the auto-debit scheduler reliability in this CMS project.

Requirements:

* Ensure the scheduler runs only once per day (no duplicate execution)
* Implement a DB-based lock mechanism (e.g., scheduler_locks table or row-level lock)
* If the job is already running or completed for the day, skip execution
* Make the scheduler safe for serverless environments (like Vercel)

Constraints:

* Do not change overall architecture
* Reuse existing runAutoDebit function
* Keep logic simple and maintainable

Output:

* SQL schema changes (if needed)
* Updated scheduler code
* Brief explanation of how duplicate execution is prevented
----
Upgrade this CMS project to feel more like a real financial system.

Implement ONLY the following:

1. Transaction lifecycle (pending → success/failed)
2. Idempotency key handling for payment APIs
3. Failure logging with reason
4. Concurrency-safe balance updates using DB locks

Do NOT add new services, queues, or external integrations.

Keep everything within current architecture.

Focus on correctness and data integrity over features.
